<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uploaded Papers</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=SF+Pro+Text:wght@400;500;700&display=swap');

        body {
            font-family: 'SF Pro Text', sans-serif;
            line-height: 1.8;
            margin: 0;
            padding: 0;
            background-color: #f5f5f7;
            color: #333;
        }

        header {
            background: #ffffff;
            text-align: center;
            padding: 80px 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        header h1 {
            font-size: 3rem;
            font-weight: 700;
            color: #1d1d1f;
            margin: 0;
        }

        header p {
            font-size: 1.25rem;
            color: #515154;
            margin-top: 15px;
        }

        main {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }

        .column {
            background: #ffffff;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .column:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
        }

        .column img {
            width: 100%;
            height: auto;
            border-radius: 12px;
            margin-bottom: 20px;
        }

        h2, h3 {
            color: #1d1d1f;
            font-size: 1.75rem;
            margin-bottom: 15px;
        }

        p {
            margin: 15px 0;
            font-size: 1rem;
            color: #515154;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .file-list {
            list-style: none;
            padding: 0;
        }

        .file-list li {
            margin: 10px 0;
        }

        .file-list a {
            color: #1d1d1f;
            text-decoration: none;
        }

        .file-list a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <header>
        <h1>Uploaded Papers</h1>
    </header>
    <main>
        <div class="column">
            <h2>View Uploaded Papers</h2>
            <ul class="file-list">
                <% files.forEach(file => { %>
                    <li><a href="/uploads/<%= file %>" target="_blank"><%= file %></a></li>
                <% }) %>
            </ul>
        </div>
    </main>
</body>
</html>