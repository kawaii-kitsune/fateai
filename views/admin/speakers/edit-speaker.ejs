<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Speaker</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #f5f5f7;
            margin: 0;
            padding: 0;
            color: #333;
        }
        .container {
            max-width: 800px;
            margin: 40px auto;
            padding: 0 20px;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #ffffff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }
        .header a {
            text-decoration: none;
            color: #007aff;
            font-weight: 500;
        }
        .header a:hover {
            text-decoration: underline;
        }
        .form {
            background: #ffffff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }
        .form h2 {
            margin: 0 0 20px;
            font-size: 20px;
            font-weight: 600;
        }
        .form input, .form textarea {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
        }
        .form button {
            padding: 10px 20px;
            background-color: #007aff;
            border: none;
            border-radius: 8px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            font-weight: 500;
        }
        .form button:hover {
            background-color: #005bb5;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Edit Speaker</h1>
            <a href="/admin">Back to Dashboard</a>
        </div>
        <div class="form">
            <h2>Edit Speaker</h2>
            <form action="/admin/speakers/edit/<%= speaker.speaker_id %>" method="POST" enctype="multipart/form-data">
                <input type="text" name="first_name" value="<%= speaker.first_name %>" placeholder="First Name" required>
                <input type="text" name="last_name" value="<%= speaker.last_name %>" placeholder="Last Name" required>
                <input type="text" name="title" value="<%= speaker.title %>" placeholder="Title" required>
                <input type="text" name="organization" value="<%= speaker.organization %>" placeholder="Organization" required>
                <textarea name="bio" placeholder="Bio" rows="5" required><%= speaker.bio %></textarea>
                <input type="text" name="photo_url" value="<%= speaker.photo_url %>" placeholder="Photo URL">
                <input type="file" name="photo">
                <button type="submit">Update Speaker</button>
            </form>
        </div>
    </div>
</body>
</html>